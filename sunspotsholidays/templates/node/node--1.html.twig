{#
/**
 * @file
 * Theme override for node 1.
 *
 * This template is used specifically for node 1 and includes
 * the React application container.
 *
 * Available variables:
 * - node: The node entity.
 * - show_react_app: Boolean indicating if React app should be shown (set by hook).
 */
#}
<div class="sunspots-holidays-node-1">
    {# React App Container - This is where the React app will mount #}
    {# data-api-url: API base URL (optional, defaults to current origin) #}
    {# data-use-mock: Set to "false" to use API instead of mock data #}
    <div id="react-app" data-api-url="{{ url('<front>') }}" data-use-mock="true"></div>
</div>

